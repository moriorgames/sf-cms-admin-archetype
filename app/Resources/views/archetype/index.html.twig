{% extends 'base.html.twig' %}

{% block body %}
    <div class="page-header">
        <h1>{{ 'map.title_index'|trans|raw }}</h1>
    </div>

    <div class="row">
        <div class="col-sm-12">

            <table class="table">
                <thead>
                <tr>
                    <th>{{ 'archetype.table.id'|trans }}</th>
                    <th>{{ 'archetype.table.name'|trans }}</th>
                    <th>{{ 'map.table.height'|trans }}</th>
                    <th>{{ 'map.table.width'|trans }}</th>
                    <th>{{ 'map.table.action'|trans }}</th>
                </tr>
                </thead>
                <tbody>
                {% for archetype in archetypes %}
                    <tr>
                        <th scope="row">{{ map.id }}</th>
                        <td>{{ archetype.name }}</td>
                        <td>{{ archetype.height }}</td>
                        <td>{{ archetype.width }}</td>
                        <td>
                            <a href="{{ path('map_edit', {id : archetype.id} ) }}">
                                {{ 'action.edit'|trans }}
                            </a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
                <tfoot>
                <tr>
                    <th>&nbsp;</th>
                    <th>&nbsp;</th>
                    <th>&nbsp;</th>
                    <th>&nbsp;</th>
                    <th>
                        <a href="{{ path('archetype_create') }}"
                           class="btn btn-primary pull-right" type="submit">{{ 'archetype.form.new_archetype'|trans }}</a>
                    </th>
                </tr>
                </tfoot>
            </table>

        </div>

    </div>
{% endblock %}
